{% set content %}
    {% for message in messages %}
        {% include 'chat/_message.html.twig' %}
    {% endfor %}

    <script>
    // Initialize chat streaming with required URLs
    const streaming = new window.ChatStreaming(
        '{{ path('app_chat_stream', {'messageId': userMessageId}) }}',
        '{{ path('app_chat_ai_response', {'messageId': userMessageId}) }}'
    );
    streaming.initializeStreaming();
    </script>
{% endset %}
{% include 'chat/_messages_container.html.twig' with {'content': content} %}